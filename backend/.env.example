# Which guidance-agent implementation to load. "mock" works offline with no API key.
AI_PROVIDER=mock

# Required only when AI_PROVIDER=openai.
# OPENAI_API_KEY=

# Optional overrides.
# AGENT_MODEL=openai:gpt-4o-mini
# AGENT_TIMEOUT_S=4.0
# ALLOWED_ORIGINS=http://localhost:5173
