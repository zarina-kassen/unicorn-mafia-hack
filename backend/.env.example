# OpenRouter Configuration (official Python SDK: pose-variants SSE — images + outlines)
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Mubit (required — app startup fails if missing or blank)
MUBIT_API_KEY=
# Optional overrides (defaults are fine for hosted Mubit)
# MUBIT_ENDPOINT=https://api.mubit.ai
# MUBIT_TRANSPORT=auto

# OpenAI Configuration (onboarding vision only — see memory_onboarding.py)
OPENAI_API_KEY=

# AI Model Configuration
# Text pose planner (not FLUX — FLUX is image-only; see FAST_IMAGE_MODEL).
AGENT_MODEL=meta-llama/llama-3.3-70b-instruct
# Pose-variant images: FLUX on OpenRouter. Legacy: IMAGE_MODEL is an alias.
FAST_IMAGE_MODEL=black-forest-labs/flux.2-klein-4b
# Vision + JSON Schema outline (not FLUX; override if you prefer another vision model).
POSE_GUIDE_MODEL=openai/gpt-4o-mini
# Keep low: OpenRouter reserves budget against this cap (default 2048 is enough for small JSON)
POSE_GUIDE_MAX_TOKENS=2048
# Pose-target planning agent (pydantic_ai); lower if you hit 402 "fewer max_tokens"
AGENT_MAX_TOKENS=8192

# Storage Configuration
GENERATED_TTL_SECONDS=21600

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Clerk Authentication
CLERK_SECRET_KEY=
CLERK_AUTHORIZED_PARTIES=http://localhost:5173,http://127.0.0.1:5173

# PostgreSQL Configuration
POSTGRES_DATABASE=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# LinkedIn post pipeline (UGC) — set LINKEDIN_DEMO_MODE=0 and fill OAuth when publishing for real
LINKEDIN_DEMO_MODE=true
# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=
# LINKEDIN_REDIRECT_URI=http://localhost:8000/api/linkedin/oauth/callback
# FRONTEND_OAUTH_LANDING=http://localhost:5173/
# Vision scoring (OpenRouter; defaults match OpenRouter config above)
# LINKEDIN_SCORE_MODEL=openai/gpt-4o-mini
# Devin API (optional — workflow falls back if unset)
# DEVIN_API_KEY=
# DEVIN_API_BASE_URL=https://api.devin.com
# Optional SQLite path (default: backend/linkedin.sqlite3)
# LINKEDIN_DB_PATH=
